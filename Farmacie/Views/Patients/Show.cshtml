@model Farmacie.Models.Patient

<a class="btn buttonmaro bi bi-arrow-left rounded-pill mb-5" href="/Patients/Index">Inapoi</a>

<div style="padding-left:10%">
    <div class="card" style="background-color: #45625d; color: #a3d2d5; padding-left: 2%; padding-top: 2%; width:80%;">
        @Html.Partial("PatientInfo", Model)
    </div>

    @if (ViewBag.AfisareButoane == true || ViewBag.UserCurent == Model.Id)
    {
        @*<div class="d-flex flex-row justify-content-between p-3">

            <a class="btn buttonsheen" href="/Patients/Edit/@Model.Id">Editeaza pacientul</a>

        </div>*@
     <div class="d-flex flex-row justify-content-between p-3" style="width:80%">
        <a class="btn buttonsheen" href="/Patients/Edit/@Model.Id">Editeaza pacientul</a>
        @if (ViewBag.AfisareButoane == true)
        {
            <form method="post" action="/Patients/Delete/@Model.Id">

                <button class="btn buttondeep" type="submit">Sterge pacientul</button>

            </form>
        }
     </div>
    }
</div>